# Welcome CLI (SSH sanity)

**Idea:** Dynamic instance: SSH in with the credentials from the contest page, read **`/flag/flag.txt`**.

**Connect:** `ssh -p 31117 ssh@133.88.122.244` (host/port/user vary per instance; use the values shown on your task page.)

**Auth:** password `cpctf2026` (per challenge briefing — rotate if the org publishes a different one).

**Flag:** `CPCTF{w31c0m3_2_c11_w0r1d}`
