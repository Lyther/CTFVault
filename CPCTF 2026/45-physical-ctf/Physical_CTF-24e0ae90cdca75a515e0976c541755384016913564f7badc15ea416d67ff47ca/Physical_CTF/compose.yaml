services:
  problem:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - FLAG=CPCTF{test_flag}
      - RP_ID=localhost
      - RP_ORIGIN=http://localhost:8080
