# Dualcast

**Idea:** The script prints `c = bytes_to_long(flag_bytes)` using PyCryptodome’s big-endian integer encoding.

**Solve:** Invert with `long_to_bytes(c)` (same module).

**Flag:** `CPCTF{wh47_7yp3_15_y0ur_477r1bu73?}`
